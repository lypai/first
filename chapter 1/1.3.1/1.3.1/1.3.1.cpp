// 1.3.1.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>//we included iostream
#include <iomanip>//we included iomanip
#include <string>//we included string

int main(void)
{
	int result = 3*60*60;
	int threemin = 3*60; // This is a variable to hold the value of 3 minutes in seconds
	int fivemin = 5*60;// This is a variable to hold the value of 5 minutes in seconds
	float pi=3.141526; //This is the value of pi
	std::cout << "result: " << result << std::endl;
	return 0;
}

